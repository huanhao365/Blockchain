constant sample{
    int a = 24;
    function sample_if_else(){
        if (a == 24){
            //如果变量a的值等于24，则执行这个中括号内的代码
        }
        else if(a == 14){
            //如果变量a不等于24但是等于14，那么执行这个中括号内的代码
        }
        else{
            //如果变量a既不等于14，也不等于24，也就是上面的两个条件
            //都不满足，那么执行这个中括号内的代码
        }
    }
}